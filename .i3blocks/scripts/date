#!/bin/sh
#DATE=$(date '+ %a %d %b  %H:%M')
DATE=$(date '+ %a  %H:%M')
#DATE=$(date '+ %H:%M')
echo $DATE

case $BLOCK_BUTTON in
    1) gsimplecal ;;
    2) chromium  --profile-directory=Default --app-id=hmjkmjkepdijhoojdojkdfohbdgmmhki ;;
    3) chromium --profile-directory=Default --app-id=ejjicmeblgpmajnghnpcppodonldlgfn ;; 
esac 
