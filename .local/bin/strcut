#!/usr/bin/env python

while(1):
    s = str(input())
    if (len(s) > 50):
       s = s[:35] + '..' + s[-15:] #cut right from center
       # s = s[:30] + '..' + s[-1:] #cut normal      
    print(s)

