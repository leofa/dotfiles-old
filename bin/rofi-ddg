#!/bin/bash

DMENU='rofi -dmenu -i -p ddgo:'
HIST=`cat ~/.cache/ddghist | $DMENU $*`

if grep -q "$HIST" "$HOME/.cache/ddghist" ; then
    echo already exists in history
else
    echo $HIST >> ~/.cache/ddghist
fi

$BROWSER https://duckduckgo.com/?q="$HIST"
