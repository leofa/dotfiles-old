#!/bin/bash

DMENU='rofi -width 55 -dmenu -i'

input="$(xsel -o > /dev/null | $DMENU -p 'finder:')"
if [ "$input" != '' ]
then
	result="$(echo "$input" | locate -e -r "$input" | $DMENU -p 'search result:' )"
	xdg-open "$result"
fi
