#!/bin/bash
# exit session

cmd=$(echo -e "exit i3wm\nkill $USER\nkill X" | rofi -width 350 -dmenu -p exit:)
case $cmd in
    exit i3wm)
        i3-msg exit
        ;;
    kill $USER)
        loginctl kill-user $USER
        ;;
    kill X)
        killall X
        ;;
esac
