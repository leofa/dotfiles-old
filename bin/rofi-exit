#!/bin/bash
# exit session

cmd=$(echo -e "exit i3\nkill user\nkill X\nlock" | rofi -width 350 -dmenu -p exit:)
case $cmd in
    'exit i3')
        i3-msg exit
        ;;
    'kill user')
        loginctl kill-user $USER
        ;;
    'kill X')
        killall Xorg
        ;;
    'lock')
        ilockit
        ;;
esac
